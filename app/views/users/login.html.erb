<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TravelEase Login</title>
  <!-- Bootstrap CSS -->
  <style>
    body {
      background-color: #f8f9fa;
    }
    .login-container {
      max-width: 420px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #ddd;
      border-radius: 30px;
      background-color: #f8f9fa; /* Same as body background */
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }
    .login-header {
      margin-bottom: 20px;
      color: #007bff; /* Blue color for the header */
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="login-container">
      <h2 class="login-header text-center">Login to TravelEase</h2>
      <%= form_tag(user_login_path, method: "post") do %>
        <div class="form-group">
          <%= label_tag :email, "Email" %>
          <%= email_field_tag :email, nil, class: "form-control", id: "email", aria_describedby: "emailHelp", placeholder: "Enter email" %>
        </div>

        <div class="form-group">
          <%= label_tag :password, "Password" %>
          <%= password_field_tag :password, nil, class: "form-control", id: "password", placeholder: "Password" %>
        </div>

        <div class="form-group form-check">
          <%= check_box_tag :rememberMe, "1", false, class: "form-check-input", id: "rememberMe" %>
          <%= label_tag :rememberMe, "Remember me", class: "form-check-label", for: "rememberMe" %>
        </div>

        <%= submit_tag "Login", class: "btn btn-primary btn-block" %>

        <div class="text-center mt-3">
          <%= link_to "Forgot password?", "#" %>
        </div>
      <% end %>

      <div class="text-center mt-2">
          <a href="/users/new" class="btn btn-link">Don't have an account? Sign up</a>
      </div>

    </div>
  </div>

</body>
</html>
